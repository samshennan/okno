# Handoff — WP1: CI workflow (2026-07-11)

## What
- Added `.github/workflows/test.yml`: on push + PR, ubuntu-latest,
  checkout → setup-node (node 22, npm cache) → `npm ci` → `npm run test:ui`.
- No linter added (per plan). No playwright install step — the suite's
  launcher falls back chrome → msedge → bundled, and GitHub runners ship
  Chrome.

## State
- Local suite green before commit (9/9).
- Verification = the pushed run itself; orchestrator watched it via
  `gh run watch` after push. If this handoff is the last one and the run
  was red, the fix direction is: add `npx playwright install chromium
  --with-deps` before the test step.

## Pending
- Nothing for WP1. Next: WP2 (service worker photo cache).
