# Handoff — WP8: docs & changelog (2026-07-11)

## What
- `CHANGELOG.md`: new `[Unreleased]` section written from `git log`
  (2ea331a..938c4de) — the 2026-07-11 touch fix plus all seven shipped WPs
  from this run, in the file's existing house style (Bug Fixes /
  Improvements / Developer Experience). No version bump, no tag, no
  release (DECISIONS_FOR_SAM #3).
- `README.md`: CI badge under the title; new Testing section between
  Manual install and Put-it-on-the-iPad (`npm test`, the three suites,
  Chrome/Edge requirement, `npm run test:ui`, stub-server note).
- Orchestrator wrote this WP directly (pure drafting from in-context
  session facts; a delegation brief would have been longer than the docs).

## State
- 25/25 green locally before commit. CI watched after push.
- THIS WAS THE LAST WP — all eight work packages in
  SESSION_PLAN_LONG_RUN_2026-07-11.md are ticked and pushed.

## Pending
- Nothing from the plan. Known small issues logged along the way (not in
  scope, candidates for a future batch): 1500ms cleanup literal vs
  configurable --transition-time mid-fade pop (see WP4 handoff);
  demo.html never applies transitionTime config; settings makes two
  separate /api/health fetches on load (could share one).
- `.planning/DECISIONS_FOR_SAM.md` unchanged — review whenever.
